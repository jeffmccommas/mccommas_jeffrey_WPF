// JavaScript
/* Jeffrey McCommas
 Nov. 16th 2013
 Activity: Functions Worksheet
 */

// Circumference - calculate the circumference of a circle

//store and display the value of PI:
var pi = Math.PI;
var calculateCircumference;
var resultString;

var textRadius = (parseFloat(prompt("Enter length of radius:" , '')));

if (!isNaN(textRadius)) {
    console.log(textRadius);
} else {
    alert("Looks like you didn't enter a number, please try again");

}




// Stung! - Bee sting function